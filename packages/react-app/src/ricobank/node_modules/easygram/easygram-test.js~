const it = require('tapzero').test
const { gram } = require('./easygram')

console.log(gram)
it('gram', t=>{
    const read = gram(`
        letter ::= "k"
    `)
    console.log(read)
    const ast = read("k")
})
